#include <stdio.h>

int main() {
  int numbers[5]={0,1,2,3,4};
  int *point = numbers;
 for(int i=0; i<5;i++){
     printf("%d \n", *point);
     point++;
 }

    return 0;
}
   //double pointer//

#include <stdio.h>

int main() {
  int number = 10;
  int *point =&number;
  int **secondpointer = &point;
   printf("%d\n", number);//value of var
   
   printf("%p\n", point);//address of pointer
  printf("%d\n", *point);//value of pointer
   
  
 printf("%p\n", secondpointer);//address of second pointer
 printf("%d\n", **secondpointer);//value of secondpointer
    return 0;
}




#include <stdio.h>
#include <stdlib.h>

int main() {
    int *arr;
    int n, new_n, i;

    // Initial size
    printf("Enter number of elements: ");
    scanf("%d", &n);

    // Allocate memory using calloc
    arr = (int *)calloc(n, sizeof(int));

    // Check allocation
    if (arr == NULL) {
        printf("Memory allocation failed!\n");
        return 1;
    }

    // Input values
    printf("Enter %d elements:\n", n);
    for (i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    // New size
    printf("Enter new size: ");
    scanf("%d", &new_n);

    // Reallocate memory using realloc
    arr = (int *)realloc(arr, new_n * sizeof(int));

    // Check reallocation
    if (arr == NULL) {
        printf("Memory reallocation failed!\n");
        …
}
return 0;



#include<stdio.h>
#include<stdlib.h>
int main(){
int n,i,marks;
int *array;
//get length
      printf("Enter the number of subjects:\n");
      scanf("%d",&n);
// memory allocation 
   array = (int*)malloc(n*sizeof(int));
   
   if(array == NULL){
       printf("Memory is not allocated\n");
       return 0;
   }
   else{
       printf("Memory allocated\n");
   }
   printf("Enter the subjects marks:\n");
   for(i=0;i<n;i++){
       scanf("%d",&array[i]);
   }
   printf("Subject marks are:\n");
for (i = 0; i < n; i++) {
    printf("Subject marks %d: %d\n", i + 1, array[i]);
}

   free(array);
   printf("Memory deleted successfully\n");
   return 0;
}